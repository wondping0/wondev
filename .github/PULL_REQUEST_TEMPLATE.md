## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The problem it solves. Link the issue if there is one. -->

## How it was verified

<!-- Paste the command and its output. Not a summary of it. -->

```
$ npm run verify

```

## Does this change generated output?

<!-- Check one. If `test/fixtures/golden/` has a diff, the answer is yes. -->

- [ ] No — `test/fixtures/golden/` is unchanged
- [ ] Yes — goldens regenerated deliberately, diff reviewed, and this needs a MINOR release

<!--
A change to generated bytes makes `wondev check` fail in every project that upgrades.
That is allowed, but it is never a patch release. See docs/versioning.md.
-->

## If this adds a target

- [ ] Output path verified against that tool's own documentation — link:
