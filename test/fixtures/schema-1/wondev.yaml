# Frozen fixture: a project as wondev 0.1.0 would have created it.
#
# Do not edit this file to make a test pass. It represents a project that already exists in
# the wild; changing it hides exactly the breakage these tests are meant to catch.
name: fixture-project
targets:
  - claude
  - agents
  - cursor
schema: 1
wondevVersion: 0.1.0
